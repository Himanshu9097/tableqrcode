{
  "name": "table-qr-food-platform",
  "version": "1.0.0",
  "description": "Multi-Tenant QR Table Ordering and POS Platform",
  "main": "backend/server.js",
  "scripts": {
    "build": "npm run build --prefix frontend && rm -rf public && cp -r frontend/dist ./public",
    "install-all": "npm install && npm install --prefix backend && npm install --prefix frontend --legacy-peer-deps",
    "backend": "node backend/server.js",
    "backend:dev": "nodemon backend/server.js",
    "frontend": "npm run dev --prefix frontend",
    "dev": "concurrently \"npm run backend\" \"npm run frontend\"",
    "seed": "node backend/seed.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mongoose": "^8.3.1"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
